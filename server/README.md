# Server

Target backend: SpacetimeDB authoritative MMO server.

Current state:
- toolchain scaffold only
- schema and module layout documented
- ready for real server initialization once SpacetimeDB is installed

Planned responsibilities:
- authentication/session bootstrap
- authoritative player movement
- chat routing
- NPC and enemy state
- quest progress
- loot and persistence

Suggested future commands once installed:
- spacetimedb init
- spacetimedb start
- spacetimedb publish
